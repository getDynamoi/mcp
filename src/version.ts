// This constant is replaced at build time by tsup (see tsup.config.ts).
// It must stay a string literal so the define replacement is reliable.
export const DYNAMOI_MCP_VERSION = "__DYNAMOI_MCP_VERSION__";
